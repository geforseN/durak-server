- [ ] remove receive|provide|withAdded usage

- implement Settled (primal attacker and defender exists)
  - [ ] Round
  - [ ] Players

- [ ] MaybeLinkedPlayer can have `left` and `right` as null, but Player can't


name base class instance as origin

https://www.yegor256.com/2017/11/14/static-factory-methods.html
interface Color {
}
class HexColor implements Color {
  private final int hex;
  HexColor(int h) {
    this.hex = h;
  }
}
class RGBColor implements Color {
  private final Color origin;
  RGBColor(int red, int green, int blue) {
    this.origin = new HexColor(
      red << 16 + green << 8 + blue
    );
  }
}
